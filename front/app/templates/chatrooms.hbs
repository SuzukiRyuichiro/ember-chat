{{page-title 'Chatrooms'}}
<LinkTo @route='index'>Go back to home</LinkTo>
<p class='text-lg'>Chatrooms available</p>

{{#each @model as |chatroom|}}
  <p><LinkTo
      @route='chatroom'
      @model={{chatroom}}
    >{{chatroom.name}}</LinkTo></p>
{{/each}}
